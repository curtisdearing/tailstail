# The base app (run.py, update_results.py, demo + live odds, learning, dashboard)
# uses ONLY the Python standard library — nothing to install.
#
# These are needed ONLY for the Monte Carlo + backtest add-ons
# (build_ratings.py, backtest.py, nflvalue/montecarlo.py):
pandas>=1.5
pyarrow>=10.0
numpy>=1.23

# Needed for the Phase 1 player-prop projection layer (nflvalue/features.py,
# nflvalue/projection.py, prop_backtest.py) -- distribution math (gamma,
# negative binomial, Poisson survival functions) for p_over/p_under:
scipy>=1.9

# Needed for real player positions (nflvalue/sources/rosters.py, Phase 1B).
# Only required to (re)build historical/rosters_weekly.parquet for NEW
# seasons; the 2019-2023 cache already on disk works without it.
nflreadpy>=0.1

# Optional, for running the test suite (tests/):
# pytest>=7.0

# To (re)download the historical data in historical/download_history.py:
# nfl_data_py>=0.3
