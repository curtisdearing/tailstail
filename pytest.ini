[pytest]
testpaths = tests
markers =
    offline: data-independent test. Runs in CI on every push. Applied automatically.
    needs_history: needs the gitignored historical/*.parquet corpus. Excluded from CI.
    needs_network: reaches a live service. Excluded from CI.
filterwarnings =
    ignore::DeprecationWarning
